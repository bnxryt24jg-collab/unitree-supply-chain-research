# 数据模型与判断口径

> 研究对象：**宇树科技股份有限公司**（曾用名：杭州宇树科技股份有限公司，688836.SH）
>
> 研究截点：**2026-09-20**
> 用途：公开资料下的供应链与合作关系研究，不构成投资建议。

## 1. 关系方向

数据中的 `subject_entity_id` 固定为宇树科技，`object_entity_id` 是关联实体：

| `relation_type` | object 相对宇树的角色 |
|---|---|
| `supplier` | 向宇树供货 |
| `customer` | 采购或使用宇树产品 |
| `partner` | 与宇树存在可验证合作 |
| `investor_or_investee` | 投资宇树；基金穿透须标记间接 |
| `peer` | 财务可比公司、产品竞品或技术对标 |

`relation_subtype` 进一步区分直接采购与终端使用、直接股东与基金穿透、正式合作与生态兼容等，防止把不同强度的事实压成同一种“关系”。

## 2. 事实状态与证据支持

`status` 描述结论状态：

- `fact`：公开证据直接支持当前结论；
- `inference`：证据支持基础事实，但关系结论含明确推断；
- `unknown`：证据不足或只能作为待核线索。

`claim_support` 描述证据是否蕴含当前结论：

- `direct`：来源直接陈述主体、关系和方向；
- `indirect`：需要有限、可复核的计算或链路推导；
- `unsupported`：来源仅证明行业归属、产品能力、投标失败或共现，不能证明关系；
- `conflicted`：可靠来源之间存在尚未解决的冲突。

约束：`fact` 不得搭配 `unsupported` 或 `conflicted`；`unsupported` 不得进入 `core`。

## 3. 一个分数、一个分层

### `confidence_score`：0–100

只回答“这条关系有多可信”，不衡量商业重要性：

```text
置信度 = 证据直接支持程度 × 30%
       + 来源权威性       × 25%
       + 独立交叉验证     × 20%
       + 实体及方向确定性 × 15%
       + 时效性           × 10%
```

| 维度 | 判定原则 |
|---|---|
| 证据直接支持程度 | 来源是否真正支持当前 claim，而非只与主题相关 |
| 来源权威性 | 法定披露、官方页面、官方采购公告优先；媒体和数据库用于补充或线索 |
| 独立交叉验证 | 按信息源头计数；转载同一稿件不算独立验证 |
| 实体及方向确定性 | 法人、上市主体、子公司、基金路径和买卖方向是否清楚 |
| 时效性 | 以最新支持证据的发布时间/获取时间及关系是否仍有效判断 |

硬性上限在加权后应用：

- 证据不蕴含当前结论：最高 40；
- 只有一个二手信息源：最高 70；
- 存在未解决来源冲突：最高 60。

官方一手来源且直接证明可进入 80–100，但不会仅因“来源是一手”自动获得高分。每条关系保存五个分项、加权原始分、命中的上限与最终分，便于复核。

### `relevance_tier`：业务重要性分层

- `core`：直接、量级较大、持续或影响主营业务；
- `supplementary`：证据成立但关系间接、比例较低、单次或仅补充背景；
- `candidate`：投标失败、生态兼容、新闻共现、产业链猜测或尚未证实。

高可信不等于高重要：例如，低比例基金穿透可以是“高置信度 + supplementary”；潜在核心零部件关系也可能是“低置信度 + candidate”。默认图谱和关系查询不把 `candidate` 展示为已确认关系，但允许显式查询。

## 4. 来源与证据字段

来源类型采用语义化枚举：`primary_regulatory`、`primary_official`、`primary_procurement`、`authoritative_media`、`general_media`、`commercial_database`、`industry_research`。

每条 `EvidenceRef` 至少保留：

- `source_name`、`source_type`、`url`；
- `published_date` 或 `retrieved_date`；
- `evidence_locator`：外部文件页码、表名、章节、标题或页面内可查找短语；
- `origin_id`：同一原始消息被转载时共用，用于独立性去重；
- `note`：访问限制、推导方法、冲突或本地研究记录位置。

本地 `data/raw/*.md` 是研究笔记，不替代外部证据定位，也不保存受限原文全文。

## 5. 实体与边界

实体保存法定名称、证券标识、别名和主体类型。上市集团、子公司、基金与终端使用单位不能互换；通过母子公司或基金建立的关系必须使用 `is_indirect=true` 并在陈述中写清链路。

研究仅使用合法可访问的公开资料，不绕过 robots、登录、付费墙、验证码、限流或其他访问控制，不提交密钥、个人数据、客户机密或未经授权材料。匿名供应商代号不会被猜测映射为上市公司。
